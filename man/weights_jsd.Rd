% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights.R
\name{weights_jsd}
\alias{weights_jsd}
\alias{weights_jsd_vanilla}
\alias{weights_fujikawa_tuned}
\alias{weights_hld_vanilla}
\alias{weights_hld}
\title{Further weight functions}
\usage{
weights_jsd(design, n, logbase, epsilon, tau, lambda = NULL, ...)

weights_jsd_vanilla(design, n, logbase, ...)

weights_fujikawa_tuned(weight_mat, epsilon = 1.25, tau = 0.5, ...)

weights_hld_vanilla(design, n, ...)

weights_hld(design, n, epsilon, tau, ...)
}
\arguments{
\item{design}{An object of class \code{fujikawa_x} or of class \code{OneStageBasket}
from the \code{baskexact} package.}

\item{n}{The sample size per basket.}

\item{logbase}{Tuning parameter. The base of the logarithm that is used to
calculate the Jensen-Shannon divergence.}

\item{epsilon}{Tuning parameter that determines the amount of borrowing.
See \code{\link[basksim]{setup_fujikawa}}).}

\item{tau}{Tuning parameter that determines how similar the baskets
have to be that information is shared. See \code{\link[basksim]{setup_fujikawa}}).}

\item{lambda}{The posterior probability threshold, currently only used
for designs with \code{"exact"} backend where pruning is activated. See
documentation of \code{baskexact::weights_fujikawa} for more information.}

\item{...}{Further arguments.}

\item{weight_mat}{An untuned matrix including the weights of all possible
pairwise outcomes.}
}
\value{
A matrix including the weights of all possible pairwise outcomes.
}
\description{
A couple of weight functions additional to the ones implemented in \code{baskexact}
are supplied. The weight functions are based on the Jensen-Shannon divergence
(JSD) and the Hellinger distance (HLD). The function \code{weights_jsd} is a
wrapper of \code{baskexact::weights_fujikawa}. It can be used with both designs
of class
\code{fujikawa_x} (from the \code{baskwrap} package) and designs of class \code{OneStageBasket}
(from the \code{baskexact} package). The function \code{weights_jsd_vanilla} is a
convenience wrapper that calls this with \code{epsilon = 1} and \code{tau = 0}
without pruning. Hence, this function returns precisely Fujikawa et al.'s
weights without any tuning. The function \code{weights_fujikawa_tuned} tunes an
existing weight matrix using the parameters \code{epsilon} and \code{tau} in accordance
with Fujikawa et al.'s tuning rules. The function \code{weights_hld} and
the "convenience wrapper" \code{weights_hld_vanilla} are a variant of Fujikawa's
weights where the similarity is calculated using 1 minus
Hellinger distance instead of 1 minus Jensen-Shannon divergence (see Details).
}
\details{
For posterior
beta distributions as in Fujikawa's design, the Hellinger distance can be
calculated "analytically", e.g. for posterior parameters \eqn{(a_1,b_1)} and
\eqn{(a_2,b_2)}, we have
\deqn{HLD(\mathrm{Beta}(a_1,b_1),\mathrm{Beta}(a_2,b_2)) = 1 - \frac{B(\frac{a_1+a_2}{2},\frac{b_1+b_2}{2})}{\sqrt{B(a_1,b_1)B(a_2,b_2)}},}
where \eqn{B(\cdot,\cdot)} is the beta function (Sasha 2012). The similarity
between strata is calculated as \eqn{1-HLD(\cdot,\cdot)}.
}
\examples{
design <- setup_fujikawa_x(k = 3, p0 = 0.2, backend = "exact")
weight_mat <- weights_jsd_vanilla(design, n = 20, logbase = 2)
weight_mat_tuned <- weights_fujikawa_tuned(weight_mat, epsilon = 1.25,
                                           tau = 0.5)
# In theory, this weights_function is also compatible with baskexact.
baskexact::toer(design$design_exact, n = 20,
                lambda = 0.95, weight_fun = weights_jsd,
                weight_params = list(epsilon = 2,
                                     tau = 0,
                                     logbase = 2))
# Use different function in get_details
get_details(design = design, n = 20, p1 = c(0.2, 0.5, 0.5), lambda = 0.95,
            epsilon = 2, tau = 0, weight_fun = weights_jsd,
            logbase = exp(1))
get_details(design = design, n = 20, p1 = c(0.2, 0.5, 0.5), lambda = 0.95,
            epsilon = 2, tau = 0, weight_fun = weights_hld,
            logbase = exp(1))
}
\references{
Sasha. Answer to "Hellinger distance between Beta distributions";
2012. Available from: https://math.stackexchange.com/a/165399/332808
}

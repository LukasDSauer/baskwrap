% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights.R
\name{weights_fujikawa}
\alias{weights_fujikawa}
\alias{weights_fujikawa.fujikawa_x}
\title{Weights Based on Fujikawa et al.'s Design}
\usage{
weights_fujikawa(design, ...)

\method{weights_fujikawa}{fujikawa_x}(
  design,
  n,
  lambda,
  epsilon = 1.25,
  tau = 0.5,
  logbase = 2,
  prune = FALSE,
  globalweight_fun = NULL,
  globalweight_params = list(),
  ...
)
}
\arguments{
\item{design}{An object of class \code{fujikawa_x}.}

\item{...}{Further arguments.}

\item{n}{The sample size per basket.}

\item{lambda}{The posterior probability threshold.}

\item{epsilon}{Tuning parameter that determines the amount of borrowing.
See \code{\link[basksim]{setup_fujikawa}}).}

\item{tau}{Tuning parameter that determines how similar the baskets
have to be that information is shared. See \code{\link[basksim]{setup_fujikawa}}).}

\item{logbase}{Tuning parameter. The base of the logarithm that is used to
calculate the Jensen-Shannon divergence.}
}
\value{
A matrix including the weights of all possible pairwise outcomes.
}
\description{
This wrapper functions returns the borrowing weights defined for Fujikawa
et al's basket trial design using the function \code{baskexact::weights_fujikawa}
(changing the attribute \code{design$backend} doesn't change anything here).
For future adaptations, this function is an S3 generic.
}
\examples{
design <- setup_fujikawa_x(k = 3, p0 = 0.2)
toer(design, n = 15, epsilon = 2, lambda = 0.99, weight_fun = weights_fujikawa)
}

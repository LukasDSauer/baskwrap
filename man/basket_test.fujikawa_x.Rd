% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/basket_test.R
\name{basket_test.fujikawa_x}
\alias{basket_test.fujikawa_x}
\title{Test for the results of a basket trial}
\usage{
\method{basket_test}{fujikawa_x}(
  design,
  n,
  r,
  lambda,
  epsilon,
  tau,
  logbase = 2,
  weight_fun = baskexact::weights_fujikawa,
  weight_params = list(epsilon = epsilon, tau = tau, logbase = logbase),
  ...
)
}
\arguments{
\item{design}{An object of class \code{fujikawa_x}.}

\item{n}{The sample size per basket.}

\item{r}{A numeric vector of the number of responses in each stratum.}

\item{lambda}{The posterior probability threshold.}

\item{epsilon}{Tuning parameter that determines the amount of borrowing.
See \code{\link[basksim]{setup_fujikawa}}).}

\item{tau}{Tuning parameter that determines how similar the baskets
have to be that information is shared. See \code{\link[basksim]{setup_fujikawa}}).}

\item{logbase}{Tuning parameter. The base of the logarithm that is used to
calculate the Jensen-Shannon divergence.}

\item{weight_fun}{Which functions should be used to calculated the pairwise
weights? Default is \code{baskexact::weights_fujikawa}.}

\item{weight_params}{A list of tuning parameters specific to \code{weight_fun}.
By default, it takes the function arguments \code{epsilon}, \code{tau} and \code{logbase}.}

\item{...}{Further arguments.}
}
\value{
A numeric vector of posterior probabilities for all strata.
}
\description{
This function is a wrapper of \code{baskexact::basket_test()}. It returns the
posterior probabilities of a basket trial including borrowing.
}
\examples{
design_x <- setup_fujikawa_x(k = 3, p0 = 0.2, backend = "exact")
basket_test(design = design_x, n = 20, r = c(2, 7, 19), lambda = 0.95,
            epsilon = 2, tau = 0,
            logbase = exp(1))
}

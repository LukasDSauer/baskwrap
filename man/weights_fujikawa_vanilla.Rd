% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weights.R
\name{weights_fujikawa_vanilla}
\alias{weights_fujikawa_vanilla}
\alias{weights_fujikawa_tuned}
\title{Weights based on Fujikawa et al.'s basket trial design}
\usage{
weights_fujikawa_vanilla(design, n, ...)

weights_fujikawa_tuned(weight_mat, epsilon = 1.25, tau = 0.5, logbase = 2, ...)
}
\arguments{
\item{design}{An object of class \code{Basket} created by
\code{setupOneStageBasket} or \code{setupTwoStageBasket}.}

\item{n}{The sample size per basket.}

\item{...}{Further arguments.}

\item{epsilon}{A tuning parameter that determines the amount of borrowing.
See details for more information.}

\item{tau}{A tuning parameter that determines how similar the baskets
have to be that borrowing occurs. See details for more information.}

\item{logbase}{A tuning parameter that determines which logarithm base is
used to compute the Jensen-Shannon divergence. See details for more
information.}
}
\value{
A matrix including the weights of all possible pairwise outcomes.
}
\description{
The function \code{weights_fujikawa_vanilla} is a wrapper of
\code{baskexact::weights_fujikawa} and calls this with \code{epsilon = 1} and \code{tau = 0}
without pruning. Hence, this function returns precisely Fujikawa et al.'s
weights without any tuning. The function \code{weights_fujikawa_tuned} tunes an
existing weight matrix using the parameters \code{epsilon} and \code{tau} in accordance
with Fujikawa et al.'s tuning rules.
}
\details{
Note that the function \code{weights_fujikawa_vanilla} changes the class of the
weight matrix in order to avoid a name clash. The output of
\code{baskexact::weights_fujikawa} is of class \code{"fujikawa"} which is identical to
the basket design class from \code{basksim}. Hence, the output of this function is
named \code{"weight_mat_fujikawa"}.
}
\examples{
design <- setup_fujikawa_x(k = 3, p0 = 0.2, backend = "exact")
weight_mat <- weights_fujikawa_vanilla(design, n = 20)
weight_mat_tuned <- weights_fujikawa_tuned(weight_mat, epsilon = 1.25,
                                           tau = 0.5, logbase = 2, ...)
}

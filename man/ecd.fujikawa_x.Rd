% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ecd.R
\name{ecd.fujikawa_x}
\alias{ecd.fujikawa_x}
\title{Calculate the Expected Number of Correct Decisions for Fujikawa et al.'s Basket Trial Design}
\usage{
\method{ecd}{fujikawa_x}(
  design,
  n,
  p1,
  lambda,
  epsilon,
  tau,
  logbase = 2,
  design_params = list(epsilon = epsilon, tau = tau, logbase = logbase),
  iter = 1000,
  data = NULL,
  weight_fun = baskexact::weights_fujikawa,
  weight_params = design_params,
  globalweight_fun = NULL,
  globalweight_params = list(),
  ...
)
}
\arguments{
\item{design}{An object of class \code{fujikawa_x}.}

\item{n}{The sample size per basket.}

\item{p1}{Probabilities used for the simulation. If \code{NULL}
then all probabilities are set to p0.}

\item{lambda}{The posterior probability threshold.}

\item{epsilon}{Tuning parameter that determines the amount of borrowing.
See \code{\link[basksim]{setup_fujikawa}}).}

\item{tau}{Tuning parameter that determines how similar the baskets
have to be that information is shared. See \code{\link[basksim]{setup_fujikawa}}).}

\item{logbase}{Tuning parameter. The base of the logarithm that is used to
calculate the Jensen-Shannon divergence.}

\item{design_params}{A list of params that is specific to the class of
\code{design}.}

\item{iter}{The number of iterations in the simulation. Is ignored if
\code{data} is specified.}

\item{data}{A data matrix with k column with the number of responses for each
basket. Has to be generated with \code{get_data}. If \code{data} is used,
then \code{iter} is ignored.}

\item{weight_fun}{Which functions should be used to calculated the pairwise
weights? Default is \code{baskexact::weights_fujikawa}.}

\item{weight_params}{A list of tuning parameters specific to \code{weight_fun}.
By default, it takes the function arguments \code{epsilon}, \code{tau} and \code{logbase}.}

\item{globalweight_fun}{Which function should be used to calculate the
global weights.}

\item{globalweight_params}{A list of tuning parameters specific to
\code{globalweight_fun}.}

\item{...}{Further arguments.}
}
\value{
A numeric value.
}
\description{
This wrapper functions returns the expected number of correct decisions
(ECD) for Fujikawa et al.'s  basket trial design. The ECD is calculated
using backends from two  different R packages:
\itemize{
\item If \code{design$backend == "sim"}, the TOER is calculated using
\code{basksim::ecd}.
\item If \code{design$backend == "exact"}, the TOER are calculated using
\code{baskexact::ecd}.
}
}
\examples{
design <- setup_fujikawa_x(k = 3, p0 = 0.2)
toer(design = design, n = 20, p1 = c(0.2, 0.5, 0.5), lambda = 0.95,
     design_params = list(epsilon = 2, tau = 0), iter = 100)
}

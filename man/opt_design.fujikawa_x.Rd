% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opt_design.R
\name{opt_design.fujikawa_x}
\alias{opt_design.fujikawa_x}
\title{Optimize Fujikawa et al.'s Basket Trial Design}
\usage{
\method{opt_design}{fujikawa_x}(
  design,
  n,
  alpha,
  design_params = list(),
  scenarios,
  prec_digits,
  iter = 1000,
  data = NULL,
  weight_fun = weights_jsd,
  weight_params = design_params,
  globalweight_fun = NULL,
  globalweight_params = list(),
  ...
)
}
\arguments{
\item{design}{An object of class \code{fujikawa_x}.}

\item{n}{The sample sizes of the baskets. A vector must be used for varying sample sizes.}

\item{alpha}{The one-sided significance level.}

\item{design_params}{A list of params that is specific to the class of
\code{design}.}

\item{scenarios}{A matrix of scenarios.}

\item{prec_digits}{Number of decimal places that are considered when
adjusting lambda.}

\item{iter}{The number of iterations in the simulation. Is ignored if
\code{data} is specified.}

\item{data}{A data matrix with k column with the number of responses for each
basket. Has to be generated with \code{get_data}. If \code{data} is used,
then \code{iter} is ignored.}

\item{weight_fun}{Which functions should be used to calculated the pairwise
weights? Default is \code{weights_jsd}.}

\item{weight_params}{A list of tuning parameters specific to \code{weight_fun}.
By default, it takes the function arguments \code{epsilon}, \code{tau} and \code{logbase}.}

\item{globalweight_fun}{Which functions should be used to calculated the
global weights? Currently, this is only supported for the exact backend.}

\item{globalweight_params}{A list of tuning parameters specific to
\code{globalweight_fun}.}

\item{...}{Further arguments.}
}
\value{
A matrix with the expected number of correct decisions.
}
\description{
This wrapper function returns the optimal tuning parameters
for Fujikawa et al.'s  basket trial design. The design is optimized
using backends from two  different R packages:
\itemize{
\item If \code{design$backend == "sim"}, the opt_design is calculated using
\code{basksim::opt_design}.
\item If \code{design$backend == "exact"}, the opt_design are calculated using
\code{baskexact::opt_design}.
}
}
\examples{
design <- setup_fujikawa_x(k = 2, p0 = 0.2, backend = "sim")
# Usually, you should increase iter in  application, e.g. to iter = 1000
opt_design(design = design,
           n = 10, alpha = 0.05,
           design_params = list(epsilon = c(1, 2), tau = c(0, 0.5)),
           scenarios = get_scenarios(design, 0.5),
           prec_digits = 3,
           iter = 50)
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_scenarios.R
\name{get_scenarios}
\alias{get_scenarios}
\title{Generate a matrix of default outcome scenarios}
\usage{
get_scenarios(design, p1)
}
\arguments{
\item{design}{An object created with one of the \code{setup} functions.}

\item{p1}{Probability under the alternative hypothesis.}
}
\value{
A matrix with \code{k} rows and \code{k + 1} columns.
}
\description{
This functions generates a matrix of \code{k+1} default outcome scenarios,
ranging from no responsive strata (global null scenarios) to all responsive
strata (global alternative scenario). It is a wrapper of the function
\code{basksim::get_scenarios}.
}
\details{
No backend switching is implemented in this function because
\code{baskexact::get_scenarios()} does precisely the same thing as
\code{basksim::get_scenarios()}.
}
\examples{
# Example for a basket trial with Fujikawa's Design
design <- setup_fujikawa_x(k = 3, p0 = 0.2)
get_scenarios(design, p1 = 0.5)
}

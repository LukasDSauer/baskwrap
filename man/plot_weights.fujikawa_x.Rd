% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_weights.fujikawa_x}
\alias{plot_weights.fujikawa_x}
\title{Plot Weight Functions of Fujikawa et al.'s Basket Trial Design}
\usage{
\method{plot_weights}{fujikawa_x}(
  design,
  n,
  r1,
  weight_fun = baskexact::weights_fujikawa,
  weight_params = list(),
  ...
)
}
\arguments{
\item{design}{An object created with one of the \code{setup_} functions from
the \code{basksim} package or the \code{baskwrap} package.}

\item{n}{The sample size per basket.}

\item{r1}{Number of responses in one basket}

\item{weight_fun}{Which function should be used to calculate the pairwise
weights.}

\item{weight_params}{A list of tuning parameters specific to
\code{weight_fun}.}

\item{...}{Further arguments.}
}
\description{
This function is a wrapper of \code{baskexact::plot_weights()}. It visualizes
the weight functions defined for Fujikawa et al.'s design.
}
\examples{
design_x <- setup_fujikawa_x(k = 3, p0 = 0.2, backend = "exact")
# Default weight function is baskexact::weights_fujikawa, which is identical
# to the Jensen-Shannon weights
plot_weights(design = design_x, n = 20, r1 = 11,
             weight_params = list(tau = 0, epsilon = c(0.25, 0.5, 1, 2)))
# Explicitly compare Jensen-Shannon and Hellinger weights
plot_weights(design = design, n = 20, r1 = 11, weight_fun = weights_jsd,
             weight_params = list(tau = 0, epsilon = c(0.25, 0.5, 1, 2),
                                  logbase = 2))
plot_weights(design = design, n = 20, r1 = 11, weight_fun = weights_hld,
             weight_params = list(tau = 0, epsilon = c(0.25, 0.5, 1, 2)))
}
